# ==============================================
# YNM SAFETY - ENVIRONMENT VARIABLES (TEMPLATE)
# ==============================================
# 1. Copy to .env.local (recommended) OR .env:
#      cp .env.example .env.local
# 2. Fill in NEW/ROTATED values only (assume old values are compromised).
# 3. NEVER commit .env.local/.env (only this .env.example is safe to commit).
# 4. For production: set vars in your host dashboard (Cloud Run/Vercel/etc).
#
# This file contains NO real secrets - placeholders only. Safe to commit.
# ==============================================

# ==============================================
# GOOGLE ANALYTICS 4 (Optional)
# ==============================================
# GA4 Measurement ID like: G-XXXXXXXXXX
NEXT_PUBLIC_GA_ID=

# ==============================================
# GOOGLE SHEETS (Required for ALL 4 forms)
# ==============================================
# Get these from Google Cloud Console:
# 1. Create a project at console.cloud.google.com
# 2. Enable Google Sheets API
# 3. Create a Service Account and download JSON key
# 4. Share your Google Sheet with the service account email (Editor role)
#
# ONE Google Sheet file with 4 tabs (sheet names):
#   1. "contact us"                 → /api/contact/submit
#   2. "our director appointment"   → /api/director-appointment/submit
#   3. "investor relations"         → /api/investor-relations/submit
#   4. "foreign collaborations"     → /api/foreign-collaborations/submit
#
# IMPORTANT: Sheet tab names must match exactly (case-sensitive).
#
GOOGLE_SHEET_ID=
GOOGLE_SERVICE_ACCOUNT_EMAIL=
# NOTE: keep the quotes; the code converts \n into real newlines.
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nREPLACE_ME\n-----END PRIVATE KEY-----\n"

# ==============================================
# GOOGLE GEMINI API (Required for AI Chatbot)
# ==============================================
# Get your API key from: https://aistudio.google.com/app/apikey

GOOGLE_GEMINI_API_KEY=

# ==============================================
# CAREERS FORM EMAIL
# ==============================================
# Gmail account to send career emails from
# Create an App Password at: https://myaccount.google.com/apppasswords
# See GMAIL_APP_PASSWORD_GUIDE.md for detailed instructions

# Your Gmail address (use App Password from https://myaccount.google.com/apppasswords)
GMAIL_USER=your-email@gmail.com
GMAIL_APP_PASSWORD=

# HR destination email (where career applications will be sent)
HR_EMAIL=ynm.hr@ynmsafety.com

# No-reply sender for automated career emails (often same as GMAIL_USER)
CAREERS_NOREPLY_FROM=your-email@gmail.com

# ==============================================
# OPTIONAL SETTINGS
# ==============================================

# reCAPTCHA v2 (Optional - for spam protection)
# Get keys from: https://www.google.com/recaptcha/admin
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=
RECAPTCHA_SECRET_KEY=

# Internal tooling (Optional)
# Used by: node scripts/verify-integrations.js
VERIFY_BASE_URL=http://localhost:3000
